# ===== CONFIGURATION ADMIN / SÉCURITÉ =====
# Mot de passe admin initial (utilisé si pas de hash persisté en DB)
# ⚠️  À CHANGER EN PRODUCTION
ADMIN_PASSWORD=MGS_Admin_2026!

# ===== CONFIGURATION EMAIL (SMTP) =====
# Pour envoyer des e-mails (confirmations newsletter, recrutement)
# Laissez vide (ou commentez) pour désactiver SMTP

# SMTP server host (ex: smtp.gmail.com, smtp.mailgun.org)
# SMTP_HOST=smtp.gmail.com
# SMTP server port (465 pour SSL, 587 pour TLS)
# SMTP_PORT=587
# SMTP username (votre adresse email ou utilisateur API)
# SMTP_USER=votre-email@gmail.com
# SMTP password (app password ou mot de passe SMTP)
# SMTP_PASS=votre-mot-de-passe-app
# Email d'expédition (par défaut SMTP_USER)
# SMTP_FROM=noreply@votre-site.com
# Email destinataire pour les candidatures (recrutement)
TO_EMAIL=ouattarabadiori5@gmail.com

# Désactiver l'envoi d'e-mails même si SMTP est configuré (utile pour staging/dev)
ENABLE_EMAILS=true

# ===== CONFIGURATION NEWSLETTER =====
# Activer le double opt-in (confirmation par email requise)
NEWSLETTER_DOUBLE_OPTIN=false

# Origine du site pour les liens de confirmation (double opt-in)
SITE_ORIGIN=http://localhost:8080

# ===== CONFIGURATION STOCKAGE S3 (OPTIONNEL) =====
# Pour uploader les fichiers vers S3 au lieu du stockage local
# Installez d'abord: npm install @aws-sdk/client-s3

# S3_BUCKET=votre-bucket-name
# S3_REGION=us-east-1
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=votre-secret-key

# ===== CONFIGURATION SCAN DE FICHIERS (OPTIONNEL) =====
# Commande shell pour scanner les fichiers uploadés (ex: antivirus)
# Utilisez {file} comme placeholder pour le chemin du fichier
# Exemple avec ClamAV: clamscan {file}
# SCAN_COMMAND=clamscan {file}

# ===== CONFIGURATION SERVEUR =====
# Port du serveur en production
# PORT=3000

# ===== CLIENT (VITE) =====
# Base API (généralement /)
VITE_API_BASE=/

# ===== CONFIGURATION LEGACY / WEB3FORMS =====
# Si vous utilisez Web3Forms pour les formulaires de contact client
VITE_WEB3FORMS_KEY=your_access_key_here
